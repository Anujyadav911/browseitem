PORT=3000

# Connection string for your PostgreSQL database (e.g., Neon, Supabase, or local)
# Replace with your actual credentials to run the project.
DATABASE_URL=postgresql://user:password@localhost:5432/browseitem?sslmode=disable
